package app

/*

import (
   "fmt"
   "net/http"
   "time"

   pkglog "github.com/jordanp/goapp/pkg/log"
)

func (a *Application) h(w http.ResponseWriter, r *http.Request) {
   time.Sleep(2 * time.Second)
   pkglog.G(r.Context()).Info("some log")
   fmt.Fprintf(w, "toto\n")
}
*/
